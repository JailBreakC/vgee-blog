define(['jquery', 'angular'], function($, angular) {
  var app;
  app = angular.module('myblog');
  return app.factory('AuthService', [
    '$http', function($http) {
      var fn;
      fn = {};
      return {};
    }
  ]);
});
