define ['jquery', 'angular'], ($, angular) ->

    app = angular.module 'myblog'

    app.factory 'AuthService', [
        '$http',
        ($http) ->
            fn = {};
            return {};
    ]

